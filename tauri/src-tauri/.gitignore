# Model files - downloaded at runtime or built separately
*.onnx
*.zip
**/models/*

# N-10 (P207)：embedding 注册表与签名、模型 zip 需随仓库入库供 raw.githubusercontent 下载。
# 注意：
# 1. `**/models/*` 必须是任意层级前缀（`models/*` 只匹配 .gitignore 所在目录的 models/，
#    无法匹配 resources/models/）；
# 2. 白名单规则同样需 `**/models/` 前缀才能覆盖 `models/*` 与 `*.zip` 全局规则
#    （gitignore 最后一条匹配规则生效）。
!**/models/registry.json
!**/models/registry.json.minisig
!**/models/*.zip
