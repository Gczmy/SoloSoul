# 09 — 跨平台材质系统与视觉规范

> **前置阅读**：`08_前端技术架构与组件映射.md`
> **Manifesto 对齐**：最少惊喜 | 用户主权
> **源文档**：`tauri_refactor/前端框架与UI设计系统.md`（材质/视觉部分）+ `tauri_refactor/跨平台页面线框与视觉规范.md`

---

## 1. 设计理念：温暖的数字堡垒

SoloSoul 管理用户最私密的数字资产，界面必须传递**安全感**与**温度感**的平衡：

- **安全 ≠ 冰冷**：暖白 + 低饱和蓝灰，像一本精心装帧的个人手账
- **隐私 ≠ 隐藏**：敏感信息用分级遮罩（毛玻璃模糊 + 渐变蒙层），不是粗暴黑块
- **专业 ≠ 复杂**：操作路径不超过三步

### 三个设计原则

```
1. 内容优先，材质服务于内容
   玻璃/云母效果只能增强信息层级，不能干扰阅读

2. 平台原生，体验统一
   各平台用各自的材质语言，但交互逻辑、信息架构完全一致

3. 降级优雅，从不破碎
   不支持新材质的平台自动获得精心设计的回退方案
```

---

## 2. 材质决策矩阵

| 平台 | 系统版本 | 首选材质 | 回退材质 | 技术实现 |
|------|---------|---------|---------|---------|
| **macOS** | 15+ (Sequoia) | Liquid Glass | Acrylic 模糊 | CSS backdrop-filter + WebGL |
| **macOS** | 14 及以下 | Acrylic 亚克力 | 纯色 + 投影 | CSS backdrop-filter: blur() |
| **iOS** | 26+ | Liquid Glass | 标准模糊 | CSS backdrop-filter |
| **iOS** | 25 及以下 | 标准模糊 | 纯色层级 | CSS 模糊或预渲染纹理 |
| **Windows** | 11 (22H2+) | Mica Alt | Mica | Tauri 原生 DWM API |
| **Windows** | 10 | Acrylic | 纯色 + 投影 | CSS backdrop-filter |
| **Linux** | 不限 | 自定义模糊 | 纯色 | CSS backdrop-filter（视 compositor） |
| **Android** | 14+ | 原生模糊 | 纯色层级 | CSS / Canvas 模糊回退 |

---

## 3. 材质层定义

```
┌─ Layer 3: Content Glass（内容玻璃层）─┐
│  卡片、弹窗、浮层 — 最高模糊 + 折射高光  │
│  blur(40px) + saturate(180%) + 内发光   │
├─ Layer 2: Toolbar Glass（工具栏层）────┤
│  侧边栏、顶部栏 — 中等模糊              │
│  blur(20px) + 细微边框                 │
├─ Layer 1: Base Surface（基底材质层）───┤
│  窗口背景 — 平台决定                    │
│  macOS=液态玻璃 / Windows=Mica / 回退=纯色│
└────────────────────────────────────────┘
```

---

## 4. 色彩系统 — "Warm Stone" 暖石调

### 明亮模式

| Token | 色值 | 用途 |
|-------|------|------|
| `--bg-base` | `#FAFAF8` | 全局画布（象牙白，比纯白更温暖） |
| `--bg-elevated` | `#FFFFFF` | 卡片、弹窗 |
| `--bg-toolbar` | `rgba(250,250,248,0.8)` | 侧边栏（半透明） |
| `--text-primary` | `#1A1A1A` | 主标题、正文 |
| `--text-secondary` | `#6B6B6B` | 辅助说明、时间戳 |
| `--text-tertiary` | `#9E9E9E` | 占位符、禁用态 |
| `--accent-primary` | `#5B7C99` | 低饱和蓝灰（信任与冷静） |
| `--accent-warm` | `#C4925C` | 温暖强调色（琥珀调） |
| `--border-subtle` | `#EBEAE6` | 分割线、卡片边框 |
| `--shadow-sm` | `rgba(0,0,0,0.04)` | 轻微投影 |
| `--shadow-md` | `rgba(0,0,0,0.08)` | 中等投影 |
| `--shadow-lg` | `rgba(0,0,0,0.12)` | 弹窗投影 |

### 暗色模式

| Token | 色值 | 用途 |
|-------|------|------|
| `--bg-base` | `#1C1C1E` | 全局画布（深灰，非纯黑，减少 OLED 频闪） |
| `--bg-elevated` | `#2C2C2E` | 卡片、弹窗 |
| `--text-primary` | `#F5F5F5` | 主文字 |
| `--accent-primary` | `#7BA3C4` | 暗色下稍亮的蓝灰 |

---

## 5. 敏感度色彩编码（4 级）

| 级别 | 图标 | 遮罩强度 | 验证要求 |
|------|------|---------|---------|
| `public` | `IconSensitivityPublic`（空心圆环，暖绿色描边） | 无遮罩 | 无需验证 |
| `internal` | `IconSensitivityInternal`（单点圆点，蓝灰色填充） | blur(2px)，点击展开 | 无需验证 |
| `sensitive` | `IconSensitivitySensitive`（实心圆点，琥珀色填充） | blur(8px) + 渐变蒙层 | 需生物识别/密码 |
| `critical` | `IconSensitivityCritical`（锁形图标，深红色填充） | blur(16px) + 深色蒙层 + 锁图标 | 需密码 + 操作确认 |

---

## 6. CSS 实现示例

### Liquid Glass 卡片

```css
.liquid-glass {
  background: linear-gradient(135deg, rgba(255,255,255,0.4) 0%, rgba(255,255,255,0.1) 100%);
  backdrop-filter: blur(40px) saturate(180%) brightness(1.1);
  -webkit-backdrop-filter: blur(40px) saturate(180%) brightness(1.1);
  border: 1px solid rgba(255,255,255,0.3);
  border-radius: 20px;
  box-shadow:
    inset 0 1px 1px rgba(255,255,255,0.4),
    0 8px 32px rgba(0,0,0,0.08);
}

/* 高光边缘 */
.liquid-glass::before {
  content: '';
  position: absolute; inset: 0;
  border-radius: inherit; padding: 1.5px;
  background: linear-gradient(180deg,
    rgba(255,255,255,0.6) 0%, rgba(255,255,255,0.1) 40%,
    transparent 60%, rgba(255,255,255,0.15) 100%);
  -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  mask-composite: exclude;
  pointer-events: none;
}
```

### 敏感度遮罩

```css
.sensitivity-sensitive {
  filter: blur(8px);
  position: relative;
}
.sensitivity-sensitive::after {
  content: ''; position: absolute; inset: 0;
  background: linear-gradient(135deg, rgba(250,250,248,0.3), rgba(250,250,248,0.1));
  backdrop-filter: blur(4px);
}

.sensitivity-critical {
  filter: blur(16px) brightness(0.9);
}
.sensitivity-critical::after {
  content: ''; position: absolute; inset: 0;
  display: flex; align-items: center; justify-content: center;
  background: rgba(250,250,248,0.6) url('/icons/sensitivity-critical.svg') no-repeat center;
  background-size: 24px 24px;
  backdrop-filter: blur(8px);
}
```

---

## 7. 图标与图像规范

### 7.1 核心规则

1. **禁止使用 Emoji 表情**  
   任何界面元素、图标占位、状态标记、空状态插图等，均不得使用 Unicode Emoji 字符（如 `🔔`、`🔒`、`🟢`、`✅` 等）。Emoji 在不同平台渲染不一致，且与 SoloSoul 专业、安全、温暖的品牌调性不符。

2. **所有需要图像表达的位置，使用高清自定义绘制图像**  
   替代 Emoji 的必须是符合 SoloSoul 品牌定位的、高清的、风格统一的绘制图像/图标：
   - **风格关键词**：安全、严谨、温暖、私密、精致
   - **视觉语言**：低饱和暖色、圆润几何、柔和投影、无尖锐边缘
   - **技术规格**：
     - 图标：SVG 矢量，统一 24×24dp 视图框，提供 1×/1.5×/2× 密度适配
     - 插图：SVG 或 2×/3× PNG，核心空状态插图宽度 ≥ 320px
     - 色彩：使用 Warm Stone 色板，禁止 Emoji 自带高饱和色

### 7.2 实现方法（业界主流）

| 场景 | 推荐方案 | 存放路径 |
|------|---------|---------|
| 功能图标 | 自定义 SVG Icon 组件库 | `src/components/icons/` |
| 敏感度标记 | 4 级专属 SVG 图标 | `src/components/icons/sensitivity/` |
| 空状态 / 引导插图 | 设计师输出 SVG/PNG | `public/illustrations/` |
| 状态徽章 | 带图标的 Badge 组件 | `src/components/ui/Badge.tsx` |
| 临时占位 | 使用 Lucide 图标并统一二次设计 | `lucide-react` + 本地封装 |

### 7.3 敏感度图标映射（示例）

Emoji 禁止 → 替换为对应 SVG 图标：

| 级别 | 原 Emoji | 替换图标名 | 说明 |
|------|---------|-----------|------|
| `public` | 🟢 | `IconSensitivityPublic` | 空心圆环，暖绿色描边 |
| `internal` | 🔵 | `IconSensitivityInternal` | 单点圆点，蓝灰色填充 |
| `sensitive` | 🟠 | `IconSensitivitySensitive` | 实心圆点，琥珀色填充 |
| `critical` | 🔴 / 🔒 | `IconSensitivityCritical` | 锁形图标，深红色填充 |

### 7.4 正例与反例

```tsx
// 错误：使用 Emoji 作为图标
<span>🔒</span>
<div>🟢 公开</div>

// 正确：使用自定义 SVG 图标
import { IconSensitivityCritical } from '@/components/icons/sensitivity';
<IconSensitivityCritical size={16} />
<Badge icon={<IconSensitivityPublic size={12} />}>公开</Badge>
```

```css
/* 错误：在 CSS 伪元素中使用 Emoji */
.sensitivity-critical::after {
  content: '🔒';
}

/* 正确：使用 SVG 背景或图标字体 */
.sensitivity-critical::after {
  content: '';
  background: url('/icons/sensitivity-critical.svg') no-repeat center;
}
```

### 7.5 文档编写说明

本文档集中历史遗留的 Emoji 标记（如 `✅` / `❌`）仅作为文档编写约定使用，不代表产品界面规范。所有进入产品界面的符号、图标、插图必须符合本章规范。

---

## 8. 密码输入框组件规范（SecurePasswordInput）

密码输入是 SoloSoul 最核心的交互之一（解锁 Vault、修改密码、导出数据等场景）。组件必须在保证安全的前提下提供良好的可用性。

### 8.1 组件结构

```
┌─ SecurePasswordInput ───────────────────┐
│  [密码输入内容...]        [👁] [?]      │
└─────────────────────────────────────────┘
         ↑                    ↑   ↑
      密码文本（遮蔽/明文）  揭示  提示
```

> 注：示意图中的 `[👁]` `[?]` 仅用于说明位置，实际实现须使用自定义 SVG 图标，禁止直接使用 Emoji。

### 8.2 揭示按钮（Toggle Visibility）

| 属性 | 规范 |
|------|------|
| **功能** | 点击切换密码遮蔽 / 明文显示状态 |
| **图标** | `Eye`（遮蔽状态，点击后显示明文）/ `EyeOff`（明文状态，点击后恢复遮蔽），Lucide 图标二次设计，统一风格 |
| **位置** | 输入框内部右侧，距右边缘 40px（位于提示按钮左侧） |
| **尺寸** | 点击热区 20×20px，图标内容 16×16px |
| **颜色** | 默认 `--text-tertiary`，hover `--text-secondary`，active `--text-primary` |
| **行为** | 默认密码遮蔽（`type="password"`）；点击切换为明文（`type="text"`）；再次点击恢复遮蔽；**输入框失焦（blur）后自动恢复遮蔽**，防止密码明文残留 |
| **可访问性** | `aria-label="显示密码"` / `aria-label="隐藏密码"`，`aria-pressed` 状态同步 |

### 8.3 提示按钮（Password Hint）

| 属性 | 规范 |
|------|------|
| **功能** | 点击展示用户设置的密码提示词，帮助回忆密码 |
| **图标** | `HelpCircle` 或自定义 `IconPasswordHint`，Lucide 图标二次设计 |
| **位置** | 输入框内部右侧，距右边缘 12px |
| **尺寸** | 点击热区 20×20px，图标内容 16×16px |
| **颜色** | 同揭示按钮 |
| **行为** | 点击后弹出 Tooltip / 小型浮层，显示密码提示词文本；提示词文本样式为 Caption（11px），`--text-secondary`，最大宽度 240px，自动换行；浮层背景 `--bg-elevated`，圆角 8px，投影 `--shadow-md`；点击外部或再次点击按钮关闭浮层；**若用户未设置密码提示词，浮层内显示"无可用提示词"；若已设置，则显示提示词内容** |
| **可访问性** | `aria-label="查看密码提示"`，`role="tooltip"` |

### 8.4 输入框内部布局（CSS 参考）

```css
.secure-password-input {
  position: relative;
}

.secure-password-input input {
  padding-right: 72px; /* 为两个按钮预留空间：12px 边距 + 20px 图标 + 8px 间距 + 20px 图标 + 12px 边距 */
}

.input-actions {
  position: absolute;
  right: 12px;
  top: 50%;
  transform: translateY(-50%);
  display: flex;
  gap: 8px;
  align-items: center;
}
```

### 8.5 正例与反例

```tsx
// 错误：无揭示按钮，用户无法确认输入内容
<input type="password" />

// 错误：使用 Emoji 作为按钮（违反图标与图像规范第 7.1 条）
<input type="password" />
<span onClick={toggle}>👁</span>

// 错误：提示按钮点击后无任何反馈（未处理空提示词状态）
<PasswordInput hint={passwordHint} onHintClick={() => {}} />

// 正确：完整实现，提示词为空时显示"无可用提示词"
<SecurePasswordInput
  value={password}
  onChange={setPassword}
  hint={passwordHint}  // undefined | null | "" 时浮层显示"无可用提示词"
  onRevealToggle={(visible) => log('password visibility changed')}
/>
```

### 8.6 安全注意事项

1. **密码明文状态不持久化**：仅存在于组件本地 state，不可写入任何全局 Store 或 localStorage。
2. **失焦自动遮蔽**：输入框 `onBlur` 后无论当前状态如何，强制恢复 `type="password"`。
3. **提示词安全边界**：提示词仅用于帮助回忆密码，**绝不可包含密码本身**；提示词内容来自 `account.enc` 中存储的 `password_hint` 字段。
4. **屏幕阅读器**：读出"密码，已遮蔽"或"密码，已显示"，提示词浮层出现时触发 `aria-live="polite"`。

---

## 9. 侧边栏导航规范（SideNavigation）

### 9.1 布局结构

侧边栏为固定窄条（**64px 宽**），采用上下分区结构。两区之间使用 `flex: 1` 弹性空白自动分隔，不依赖硬编码高度。

```
┌─ SideNavigation（64px）─────────────────┐
│                                         │
│  [IconHome]         主页（名称卡片）      │
│  [IconProfile]      个人资料             │
│  [IconTravel]       旅行                │
│  [IconFinancial]    财务                │
│  [IconProfessional] 职业                │
│                                         │
│  ─────── flex: 1 弹性分隔 ─────────────  │
│                                         │
│  [IconLock]         锁定                │
│  [IconSearch]       搜索                │
│  [IconPlugin]       插件                │
│  [IconChat]         AI 对话              │
│  [IconSettings]     设置                │
│                                         │
└─────────────────────────────────────────┘
              ↑
       名称卡片从按钮右侧滑出
```

### 9.2 上下分区规则

| 分区 | 位置 | 包含页面 | 说明 |
|------|------|---------|------|
| **Primary Nav（上区）** | 顶部紧接 AppBar | 主页、个人资料、旅行、财务、职业 | 用户核心数据对象对应的默认页面 |
| **Secondary Nav（下区）** | 底部对齐 | 锁定、搜索、插件、AI 对话、设置 | 工具性功能入口，不直接展示数据内容 |

**注意**：
1. 上下分区是固定规则，新增页面时必须根据功能属性归入对应分区，禁止随意混排。
2. **下区按钮顺序固定（从上到下）**：锁定 → 搜索 → 插件 → AI 对话 → 设置。
3. **设置按钮始终位于下区最底部**。后续新增功能按钮时，首先保证设置按钮保持在最下方，新增按钮插入到设置按钮上方。

### 9.3 悬停名称卡片效果（Warp 风格）

鼠标悬停在侧边栏任意按钮上时，从按钮右侧滑出名称卡片：

| 属性 | 规范 |
|------|------|
| **触发** | `mouseenter` / `focus` |
| **动画** | 水平向右滑出 + 淡入（`opacity: 0→1`，`translateX: -4px→0`） |
| **缓动** | `ease-spring`（`cubic-bezier(0.34, 1.56, 0.64, 1)`） |
| **持续时间** | 200ms（进入），150ms（离开） |
| **卡片位置** | 按钮右侧，水平间距 8px，垂直居中对齐 |
| **卡片尺寸** | 自适应文本宽度，内边距 `8px 12px` |
| **卡片样式** | 背景 `Toolbar Glass`（`blur(20px)` + `saturate(180%)`），圆角 8px，阴影 `--shadow-md`，边框 `1px solid rgba(255,255,255,0.2)` |
| **文字样式** | Body（14px），`--text-primary`，`font-weight: 500` |
| **关闭** | `mouseleave` / `blur` 后淡出 |

### 9.4 按钮状态规范

| 状态 | 图标颜色 | 背景 | 左侧指示条 |
|------|---------|------|-----------|
| 默认 | `--text-secondary` | 透明 | 无 |
| 悬停 | `--text-primary` | `rgba(0,0,0,0.04)`（明亮）/ `rgba(255,255,255,0.06)`（暗色） | 无 |
| 选中（当前页） | `--accent-primary` | `rgba(91,124,153,0.08)` | 左侧 3px 竖条，`--accent-primary`，圆角 `0 2px 2px 0`，高度 20px |

### 9.5 CSS 实现参考

```css
.side-navigation {
  width: 64px;
  display: flex;
  flex-direction: column;
  padding: 12px 8px;
  gap: 4px;
}

.nav-primary,
.nav-secondary {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.nav-divider {
  flex: 1;
  min-height: 16px;
}

.nav-button {
  width: 48px;
  height: 48px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  transition: background var(--duration-fast);
  color: var(--text-secondary);
}

.nav-button:hover,
.nav-button:focus-visible {
  background: rgba(0, 0, 0, 0.04);
  color: var(--text-primary);
}

.nav-button.active {
  background: rgba(91, 124, 153, 0.08);
  color: var(--accent-primary);
}

.nav-button.active::before {
  content: '';
  position: absolute;
  left: -8px;
  top: 50%;
  transform: translateY(-50%);
  width: 3px;
  height: 20px;
  border-radius: 0 2px 2px 0;
  background: var(--accent-primary);
}

/* 名称卡片（Warp 风格） */
.name-card {
  position: absolute;
  left: calc(100% + 8px);
  top: 50%;
  transform: translateY(-50%) translateX(-4px);
  opacity: 0;
  pointer-events: none;
  padding: 8px 12px;
  border-radius: 8px;
  font-size: 14px;
  font-weight: 500;
  color: var(--text-primary);
  background: var(--bg-toolbar);
  backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.2);
  box-shadow: var(--shadow-md);
  white-space: nowrap;
  transition:
    opacity 200ms var(--ease-spring),
    transform 200ms var(--ease-spring);
}

.nav-button:hover .name-card,
.nav-button:focus-visible .name-card {
  opacity: 1;
  transform: translateY(-50%) translateX(0);
}
```

### 9.6 可访问性

- 所有导航按钮必须有 `aria-label`，值为对应页面名称。
- 键盘 `Tab` 聚焦时同样触发名称卡片显示。
- 当前选中项使用 `aria-current="page"`。
- 名称卡片使用 `role="tooltip"`。

### 9.7 正例与反例

```tsx
// 错误：上下区混合放置
<SideNavigation>
  <NavButton icon={<IconHome />} to="/">主页</NavButton>
  <NavButton icon={<IconSearch />} to="/search">搜索</NavButton>  {/* 搜索应放在下区 */}
  <NavButton icon={<IconProfile />} to="/profile">个人资料</NavButton>
</SideNavigation>

// 错误：名称卡片使用 Emoji
<NameCard>主页</NameCard>  {/* 正确：仅文字，无图标 */}

// 正确：上下分区 + Warp 风格名称卡片
<SideNavigation>
  <NavPrimary>
    <NavButton icon={<IconHome />} label="主页" to="/" />
    <NavButton icon={<IconProfile />} label="个人资料" to="/profile" />
    <NavButton icon={<IconTravel />} label="旅行" to="/workspace/travel" />
    <NavButton icon={<IconFinancial />} label="财务" to="/workspace/financial" />
    <NavButton icon={<IconProfessional />} label="职业" to="/workspace/professional" />
  </NavPrimary>
  <NavDivider />
  <NavSecondary>
    <NavButton icon={<IconLock />} label="锁定" action="lockVault" />
    <NavButton icon={<IconSearch />} label="搜索" to="/search" />
    <NavButton icon={<IconPlugin />} label="插件" to="/plugins" />
    <NavButton icon={<IconChat />} label="AI 对话" to="/llm-chat" />
    <NavButton icon={<IconSettings />} label="设置" to="/settings" />
  </NavSecondary>
</SideNavigation>
```

---

## 10. 修改密码页面规范（ChangePasswordPage）

修改密码页面位于 `设置 → 安全设置 → 修改密码`。页面须同时支持**修改密码**和**单独修改密码提示**两种操作模式。

### 10.1 页面结构与输入框

页面包含 4 个输入框，全部使用 `SecurePasswordInput` 组件（含揭示按钮 + 提示按钮）：

| 顺序 | 输入框 | 组件 | 必填 | 说明 |
|------|--------|------|------|------|
| 1 | 当前密码 | `SecurePasswordInput` | 是 | 验证当前用户身份，错误时输入框下方显示红色错误文本 |
| 2 | 新密码 | `SecurePasswordInput` | 条件 | 为空且确认新密码也为空时，执行"仅修改提示词"模式 |
| 3 | 确认新密码 | `SecurePasswordInput` | 条件 | 必须与新密码完全一致 |
| 4 | 密码提示 | `SecurePasswordInput` | 否 | 纯文本输入（非密码类型），允许为空；用于帮助回忆密码 |

**布局**：4 个输入框垂直堆叠，间距 16px，每个输入框上方有 Label（12px，`--text-secondary`，`font-weight: 500`）。

### 10.2 密码要求提示

在新密码输入框的 Label 右侧放置密码要求提示：

```
新密码                    [ℹ] 密码长度不少于 8 位
[****************] [👁] [?]
```

- **图标**：`Info` 或 `AlertCircle`（Lucide 图标二次设计），16×16px，`--text-tertiary`
- **文本**："密码长度不少于 8 位"
- **样式**：Caption（11px），`--text-tertiary`，与图标水平排列，间距 4px
- **位置**：Label 右侧，或输入框下方（与错误提示不冲突）
- **可访问性**：`aria-describedby` 将提示文本关联到新密码输入框

### 10.3 单独修改密码提示逻辑

页面须支持"仅修改密码提示"模式，无需同时修改密码：

```
当前密码：    [********]        ← 必须正确
新密码：      [        ]        ← 留空
确认新密码：  [        ]        ← 留空
密码提示：    [我的提示词]      ← 填写新提示词

[保存更改]                        ← 按钮可用
```

**保存规则**：

| 当前密码 | 新密码 | 确认新密码 | 密码提示 | 操作 |
|----------|--------|-----------|----------|------|
| 错误 | — | — | — | 拒绝，显示"当前密码错误" |
| 正确 | 空 | 空 | 任意 | **仅更新密码提示**（允许为空，即清除提示词） |
| 正确 | 有值 | 空 | 任意 | 拒绝，显示"请确认新密码" |
| 正确 | 空 | 有值 | 任意 | 拒绝，显示"请填写新密码" |
| 正确 | 有值（<8位） | 有值（<8位） | 任意 | 拒绝，显示"密码长度不少于 8 位" |
| 正确 | 有值 | 有值（不一致） | 任意 | 拒绝，显示"两次输入的密码不一致" |
| 正确 | 有值（≥8位） | 有值（一致） | 任意 | **同时修改密码和密码提示** |

### 10.4 保存按钮状态

- **禁用状态**（`disabled`）：当前密码为空 / 当前密码正在验证中
- **可用状态**（`enabled`）：当前密码验证通过，且满足上述 10.3 表格中的任一允许操作
- **加载状态**：点击保存后显示加载指示器，防止重复提交

### 10.5 错误提示规范

| 错误场景 | 显示位置 | 错误文本 |
|----------|---------|---------|
| 当前密码错误 | 当前密码输入框下方 | "当前密码不正确，请重新输入" |
| 新密码少于 8 位 | 新密码输入框下方 | "密码长度不少于 8 位" |
| 新密码与确认密码不一致 | 确认新密码输入框下方 | "两次输入的密码不一致" |
| 仅填新密码或确认密码其中之一 | 留空的输入框下方 | "请同时填写新密码和确认密码" |

错误文本样式：Caption（11px），`#C45B4A`（错误红色，对应 `--error` token），与输入框间距 4px。

### 10.6 成功反馈

- 仅修改密码提示：Toast 提示"密码提示已更新"
- 同时修改密码：Toast 提示"密码已修改，请使用新密码重新登录"
- 操作成功后清空所有输入框

### 10.7 正例与反例

```tsx
// 错误：缺少密码要求提示
<Label>新密码</Label>
<SecurePasswordInput value={newPassword} onChange={setNewPassword} />

// 错误：提示按钮未展示提示词或无可用提示词反馈
<SecurePasswordInput hint={undefined} showHintButton={false} />

// 错误：未支持单独修改密码提示（新密码为空时拒绝保存）
if (!newPassword || !confirmPassword) {
  throw new Error('新密码不能为空');  // 错误：应允许仅修改提示词
}

// 正确：完整实现，支持双模式
function handleSave() {
  if (!currentPasswordValid) return;

  const changePassword = newPassword && confirmPassword;
  const changeHint = passwordHint !== originalHint;

  if (!changePassword && !changeHint) {
    showToast('没有需要保存的更改');
    return;
  }

  if (changePassword) {
    if (newPassword.length < 8) {
      setError('newPassword', '密码长度不少于 8 位');
      return;
    }
    if (newPassword !== confirmPassword) {
      setError('confirmPassword', '两次输入的密码不一致');
      return;
    }
  }

  ipc.changePassword({
    currentPassword,
    newPassword: changePassword ? newPassword : undefined,
    passwordHint: changeHint ? passwordHint : undefined,
  });
}
```

---

## 11. 排版与间距

| 层级 | 字号 | 字重 | 用途 |
|------|------|------|------|
| Display | 32px | 700 | 空状态标题 |
| H1 | 24px | 600 | 页面标题 |
| H2 | 18px | 600 | 区块标题 |
| Body | 14px | 400 | 正文 |
| Label | 12px | 500 | 表单标签 |
| Caption | 11px | 400 | 时间戳 |

**字体栈**：
```css
--font-sans: -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI",
             "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;
--font-mono: "SF Mono", "Cascadia Code", "Fira Code", monospace;
```

**间距（8px 基线）**：4px / 8px / 12px / 16px / 24px / 32px / 48px

**圆角**：4px(标签) / 8px(按钮) / 12px(标准卡片) / 16px(大卡片) / 24px(液态玻璃面板) / 999px(胶囊)

---

## 12. 动效系统

```css
--ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1); /* 弹性 */
--ease-smooth: cubic-bezier(0.4, 0, 0.2, 1);       /* Material 标准 */

--duration-fast: 100ms;    /* 悬停、聚焦 */
--duration-normal: 200ms;  /* 按钮按下、小状态切换 */
--duration-slow: 300ms;    /* 页面切换、弹窗 */
```

---

## 13. 可访问性

| 要求 | 实现 |
|------|------|
| 键盘完全可操作 | Tab 导航、Enter/Space 激活、Esc 关闭 |
| 屏幕阅读器 | ARIA label、`role="dialog"`、live region |
| 对比度 | 正文 ≥ 4.5:1，大号文字 ≥ 3:1 |
| 焦点可见 | 2px `--accent-primary` 焦点环 |
| 动画降级 | `prefers-reduced-motion` → 0ms |
| 透明降级 | `prefers-reduced-transparency` → 纯色 |
| 触控目标 | 移动端 ≥ 44×44px，桌面端 ≥ 32×32px |

```css
@media (prefers-reduced-transparency: reduce) {
  .liquid-glass, .acrylic, .mica-overlay {
    background: var(--bg-elevated) !important;
    backdrop-filter: none !important;
  }
}
```

---

## 14. 完成标准

- [ ] 明亮/暗色模式全平台切换无闪烁
- [ ] macOS 15+ 上窗口有液态玻璃质感
- [ ] Windows 11 上 Mica 材质正确
- [ ] 开启"减少透明度"后优雅退化
- [ ] 敏感度 4 级遮罩效果正确
- [ ] 所有平台对比度 ≥ 4.5:1
- [ ] 键盘可导航所有交互元素
- [ ] 产品界面无任何 Emoji，全部使用自定义 SVG 图标/插图
- [ ] 密码输入框包含揭示按钮（Eye/EyeOff 图标），点击切换遮蔽/明文
- [ ] 密码输入框包含提示按钮（HelpCircle 图标），未设置提示词时显示"无可用提示词"
- [ ] 密码输入框失焦后自动恢复遮蔽状态
- [ ] 密码明文状态不持久化到任何 Store
- [ ] 侧边栏上下分区正确（主页/个人资料/旅行/财务/职业在上区，搜索/设置在下区）
- [ ] 侧边栏悬停时显示 Warp 风格名称卡片（滑出 + 淡入动画）
- [ ] 侧边栏当前页面有左侧高亮指示条
- [ ] 修改密码页面包含 4 个输入框（当前密码、新密码、确认新密码、密码提示）
- [ ] 修改密码页面展示密码要求提示（不少于 8 位，配合信息图标）
- [ ] 修改密码页面支持单独修改密码提示（不填新密码时仅更新提示）

---

*文档版本：v1.5*
*创建日期：2026-06-05*
*最后更新：2026-06-05*
*对应开发阶段：Phase 2（视觉系统）*
