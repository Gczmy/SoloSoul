# SoloSoul 代码复审记录

日期：2026-09-17，轮次 6。执行依据：[review_code_process.md](review_code_process.md)。完整问题台账：[CODE_ANALYSIS_REPORT.md](CODE_ANALYSIS_REPORT.md)。

## 本轮状态：本次修复完成，历史 P012 延期

以 `39e43634` 为审查基线，本轮原发现 **13 项问题：9 项 P1、4 项 P2**。P037 已随分析报告交付；本次修复 P038–P049 共 12 项，以及复核新增的 P050（Windows 测试依赖 HOME）和 P051（插件集成测试使用用户目录），**共 14 项，每项一个独立本地提交**。历史 P012 未收到改变兼容性的选择回复，沿用延期。旧清单合并后共 51 项，其中 50 项关闭（含设计例外），仅 P012 未关闭；不生成 `code-audit-passed` 标签。

分析报告交付时，首次提交推送曾因授权范围被自动审批拒绝，操作没有执行。随后用户明确要求“提交推送”，P037 的两个签名脚本、假密钥回归测试和两份审查报告提交到 `main` 并推送至 `origin/main`。后续逐项修复进度见下表和完整台账。

## 本轮发现

| ID | 等级 | 结果及影响 | 状态 |
|----|------|------------|------|
| P037 | P1 | 发布详细日志和 signer 参数暴露更新签名私钥 | 已修复，与本轮报告一同交付 |
| P038 | P1 | 共享校验拒绝点路径与 Windows 尾点别名，保存/读取/卸载均在访问前拒绝 | 已修复；插件测试 58 通过、2 原有忽略 |
| P039 | P1 | 8 个 Store、加载链及插件/同步事件使用会话与请求代次守卫 | 已修复；203 项 Store/IPC 回归，最终边界 39 项通过 |
| P040 | P1 | 冲突数据加密读写、独立迁移及原子换钥；清理当前库空闲页和 WAL | 已修复；存储测试 176 项通过 |
| P041 | P1 | 聊天纳入原子换钥；密钥恢复探测覆盖聊天、冲突及其他加密域 | 已修复；存储 178 / 核心 205 项测试通过 |
| P042 | P1 | 单字段及批量接口共用字段投影，保留 roles 和用户映射并隔离模板 | 已修复；插件 63 项通过、2 原有忽略，严格 Clippy 通过 |
| P043 | P1 | 账户 ID 在创建、删除、读写配置及解锁前统一校验，非法清单条目被过滤 | 已修复；共享核心 204 项测试通过 |
| P044 | P1 | 普通/动态字段及快照共用掩码，关键字段主密码验证与迟到结果隔离 | 已修复；16 项组件测试、类型和静态检查通过 |
| P048 | P1 | 缓存、Wasm 宿主调用、事件和最终结果均复核锁定及会话期限 | 已修复；插件 68 项通过、2 原有忽略，严格 Clippy 通过 |
| P045 | P2 | 密钥实现真实析构擦除，配置在 open 时移交密钥并移除副本 | 已修复；存储 180 项及严格 Clippy 通过 |
| P046 | P2 | 旧页面保留稳定 ID，按差集补齐，部分失败与旧删除记录均保留来源 | 已修复；25 项测试及类型、静态检查通过 |
| P047 | P2 | 私有工作区由 worker 持有并自动清理，附件私有写入成功后原子替换 | 已修复；插件 70 / 核心 206 项通过，Unix 权限待跨平台 CI |
| P049 | P2 | History 所有权及清理均改为稳定 ID，兼容结构化克隆 | 已修复；11 项 hook 回归通过，链接结束后相册 12 项单独复跑通过 |
| P050 | P2 | 生物识别可用性测试使用独立临时目录，不再依赖 HOME | 已修复；共享核心 206 项与严格 Clippy 通过 |
| P051 | P2 | 安装及构造测试使用独立临时目录，校验落盘、卸载及清理 | 已修复；3 项实际执行通过，1 项缺少示例 Wasm 自行跳过；严格 Clippy 通过 |

完整台账逐项记录位置、触发条件、修复及验证结果。每项修复均单独提交代码和报告；原有未提交改动保持原状。

## 修复执行验证（2026-09-17，Windows）

- 后端全工作区 fmt、含测试目标的严格 Clippy 通过；测试程序完整构建通过。
- 执行 GUI 459、存储 180、插件 70、同步 64、密码学 23 项均通过。插件有 2 项原有忽略，P025 大数据基准 1 项原有忽略；插件安装/地址格式/注册表/沙箱集成共 8 项返回通过，其中 hello-world 示例缺少 Wasm 产物而自行跳过。WebDAV 9 项返回通过，但未配置真实服务的分支自行跳过，不能计为真实服务验证。
- 共享核心首次 206 项中 205 通过，1 项因 HOME 缺失失败；P050 修复后核心 206 项全部通过，core 含测试目标的严格 Clippy 通过。
- GUI 测试原程序缺 Windows Common Controls v6 清单，会以 `0xc0000139` 退出；仅复制测试程序并嵌入应用现有清单，当前代码的 459 项已执行通过。未修改产品或系统配置。
- 前端 TypeScript、全量 ESLint 通过；P044 16、P046 25、P049 hook 11 项通过。P039 Store/IPC 203 项通过，最终边界补充后的会话与 IPC 39 项通过；全量 Vitest 120 文件 / 1014 项全部通过。
- 相册首项曾在后端链接并行时停留懒加载 fallback 超时；链接结束后单独复跑 12 项全通过，未放宽超时或改产品逻辑。保留这两次失败记录，不把资源竞争推断当成已证明的唯一根因。
- Markdown 分块（13 个依赖）、ACL（211 命令）、偏好键（22 键）检查通过。Unix 0700/0600 专用测试需跨平台 CI；本机没有 macOS/Android 原生效果或真实云服务验证。
- P051 修复后的安装/沙箱两个集成目标通过；安装、卸载、独立初始化和清理均限定于 TempDir。比较运行前后的原用户插件目录和审计日志元数据，无变化。

## 分析阶段验证（以下保留原审查环境结果）

| 检查 | 结果 |
|------|------|
| TypeScript、Rust fmt、Clippy、ESLint | 通过 |
| Tauri workspace `cargo test` | 允许本地回环监听后通过；GUI 460 项已执行 |
| 前端 Vitest | 关闭 Node 实验性 Web Storage 后，117 文件 / 975 用例通过 |
| Markdown 分块 / ACL / 偏好键 | 通过；13 个 Markdown 依赖、211 命令、22 键 |
| 独立 CLI | fmt、Clippy 通过；166 单元测试 + 2 集成测试通过，1 文档示例忽略 |
| P037 假密钥回归 | 3 个测试 / 14 个子场景通过；bash 语法和 diff 检查通过 |
| P038 删除边界复现 | 独立 Rust 临时沙箱确认 `..` 删除假同级账户文件 |
| P039/P046 store 复现 | 对真实 store 注入延迟响应/部分失败，确认缓存回填和迁移失败 |
| P044 展示复现 | 真实组件 SSR 注入合成 critical 字段，确认默认输出明文 |
| P049 History 语义 | 隔离 Chrome 空白页确认对象克隆；未在 Android 真机运行 |
| crate 本地依赖图 | 无循环依赖 |

保留失败过程：首次 Rust 测试 5 项因沙箱禁止监听端口失败；首次 Vitest 因运行时 Web Storage 冲突出现 42 个失败用例和 3 个未处理错误。修正运行条件后通过，未为环境故障改产品逻辑。真实 WebDAV 服务未配置，不能把自行跳过的外部服务分支计成真实服务验证。

## 覆盖、备份与未验证范围

分析阶段扫描跟踪的前端 TS/TSX/CSS 664 文件、GUI Rust 96 文件、共享 Rust 98 文件、CLI Rust 79 文件，并审查构建发布脚本、CI、capabilities 与关键调用链。跳过依赖与生成目录；未对每行代码逐行审计，未完成原生 UI、真实恢复攻击或依赖漏洞数据库专项审计。本次没有读取用户真实 Vault、私钥或附件；安全缺陷回归使用临时假数据，但既有插件集成测试存在下述真实目录副作用。

**P051 执行影响**：全量回归执行的既有安装测试调用默认 `PluginManager::new()`，在用户主目录 `.solosoul/plugins/com.solosoul.official.phone-fmt` 执行了卸载和安装，并更新插件审计日志。随后仅查看相关文件元数据确认更新时间，没有读取 Vault 数据。缺少运行前插件状态记录，无法确定是否替换了既有安装，因此保留现状并告知用户，不猜测删除或恢复。该测试与沙箱构造测试现已改用独立临时目录，隔离后的验证没有再次改变相关用户文件的元数据。

原分析阶段的备份位于 `/private/tmp/solosoul-audit-20260917/.backup/`，日志使用同名前缀；这些路径不代表当前 Windows 执行证据。本次 Windows 原有插件市场子模块、Cargo.toml、三个 NSIS 位图与搜索索引改动保持原状，不纳入修复提交。结果记录后清理本次临时备份、诊断脚本、日志及 GUI 测试程序副本。本次只做本地逐项提交，未推送。

---

## 历史记录：2026-09-10（以下内容不代表本轮结果）

## 结论

本轮新增的 P031–P036 已修复，P010/P018–P021 已按代码实现与既有处置决定校正状态。36 项清单中 35 项关闭，P012 保持延期。新增修复均已逐项提交；完整验收尚未通过：GUI Rust 测试程序无法启动，不能将其他检查成功替代该项验证。本轮未创建 `code-audit-passed` 标签。

## 本轮实际修复

| ID | 行为与影响 | 独立提交 |
|----|------------|----------|
| P031 | 权限 helper 的 Unix 专用参数不再阻断 Windows Clippy | `fa7d0974` |
| P032 | 7 个模块的 8 个异步订阅在卸载后也能释放；目录同步进度正确处理 StrictMode、语言切换及连续操作 | `7b1f6b0d` |
| P033 | 修正 CLI 生物识别导入顺序，恢复 rustfmt 检查 | `ec52e840` |
| P034 | Windows Python 检查可找到解释器并输出中文；检查失败的退出码原样保留 | `f5066336` |
| P035 | 相册 3 个懒加载用例的总超时覆盖内部等待，避免提前终止及后续用例串扰 | `48171822` |
| P036 | 清除两处测试未用变量/导入，定向严格 Clippy 通过 | `01357410` |

历史状态校正也各自独立提交：P010 `ed0db797`、P018 `581f40cf`、P019 `f4b02a58`、P020 `8127d9e0`、P021 `97e1af24`。这些提交记录已完成的拆分或设计保留，不计为本轮新增代码重构。

## 验证证据

| 检查 | 结果 |
|------|------|
| TypeScript、ESLint | 通过 |
| 最终 Vitest（maxWorkers=2） | 111 文件 / 939 用例全部通过 |
| P032 回归 | 原实现 3 个用例先复现失败；修复后 8 个新增用例通过 |
| Rust workspace fmt、Clippy | 通过；P036 两份测试追加定向 `-D warnings` 检查通过 |
| Rust 非 GUI workspace 测试 | 527 passed / 0 failed / 3 ignored |
| 存储大数据集基线 | 随后显式 `--ignored` 运行，1/1 通过 |
| GUI Rust 测试 | 编译成功；测试程序及直接 `--list` 均以 `0xc0000139 (STATUS_ENTRYPOINT_NOT_FOUND)` 退出，尚未执行用例 |
| CLI | fmt 通过；本轮只有导入排序调整，未运行 CLI 完整行为测试 |
| ACL / 偏好键 | 205 命令 / 20 键通过 |
| Markdown 分块 / 双语键 | 通过 / 双语均 0 缺失 |
| Python 启动器 | 语法与实际检查通过；注入 exit 7 后保持退出码 7 |

最初 Vitest 出现 worker 启动超时，降低并发后通过；P032 全量回归另复现相册的 5 秒超时，P035 修复后最终全量通过。保留失败过程，未以重试结果覆盖根因记录。

非 GUI Rust 统计包含 WebDAV 测试目标的 9 个成功返回，但其中需要外部服务的分支因未设置 `SOLOSOUL_WEBDAV_E2E_URL` 自行跳过，不能视为真实 WebDAV 服务端到端验证。527 的统计亦不包含被排除的 `solo_soul` GUI 测试。

## 复审范围与遗留

对 `tauri/src`、`tauri/src-tauri/src`、`tauri/crates`、`solosoul_cli/src` 做静态/启发式扫描，重点阅读旧未关闭项及其调用链，检查事件生命周期、锁与事务、附件路径和权限、Recovery、WebDAV、敏感字段展示及系统文件对话框。跳过依赖与生成目录。本轮不是对每个源文件逐行审计，也未完成原生 GUI、移动端及跨平台端到端验证。

- **P012（P2，沿用延期决定）**：Recovery 手动路径仍允许不提供主机指纹，且兼容裸 PIN。可信预期指纹缺失时，PIN/nonce/限流不能防御主动中间人。旧报告将强制指纹核验列为交互变更，本轮未收到改变该决定的答复，未标为修复。
- **GUI 验证阻断**：需定位测试程序缺失的 DLL 入口，再补跑完整 `cargo test` / `npm run check-all`。已确认直接运行也失败；未通过猜测修改加密、OCR、运行库依赖或系统配置。

## 工作区与交付

仅按明确路径提交本轮修复。启动前已有的插件市场子模块指针、三个 NSIS 位图和搜索索引改动保持未提交。无用临时备份、诊断脚本和日志在记录要点后清理。

远程推送未执行：自动审批拒绝了 `git push origin main`，理由为当前授权未明确覆盖将具体报告/项目内容发送至 `github.com/Gczmy/SoloSoul` 的 `main`。本地提交已可审查，等待具体推送授权。
