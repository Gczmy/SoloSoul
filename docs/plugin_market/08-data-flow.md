## 9. 权限与数据流完整流程

```
┌──────────┐     1. 安装      ┌─────────────────────┐
│  用户    │ ───────────────▶ │ PluginInstaller     │
└──────────┘                  │ Service             │
                              └──────┬──────────────┘
                                     │
                                     ▼ 2. 从远程/本地获取
                              ┌──────────────┐
                              │ 下载 wasm +  │
                              │ manifest     │
                              └──────┬───────┘
                                     │
                                     ▼ 3. SHA-256 校验
                              ┌──────────────┐
                              │ 校验白名单    │
                              │ 版本兼容性    │
                              └──────┬───────┘
                                     │
                        否 ◄─────────┘ 通过
                        │
                        ▼
                 ┌──────────────┐
                 │ 拒绝安装      │
                 │ 提示安全风险   │
                 └──────────────┘

通过 ──▶ 4. 解压到 ~/.solosoul/plugins/{plugin_id}/
           │
           ▼
    ┌──────────────┐
    │ 用户点击运行  │
    └──────┬───────┘
           │
           ▼
    ┌──────────────┐
    │ 创建 Session │ ◄── 生成 sess_id，设置 TTL（默认 5 分钟）
    │ 初始化 Wasm  │      内存 mlock，Fuel 限制 10_000_000
    │ 沙盒         │
    └──────┬───────┘
           │
           ▼ 5. Plugin 调用 solosoul_request_field("identity.full_name")
    ┌──────────────┐
    │ Rust Host    │
    │ 速率限制检查  │ ◄── 同一字段 10 次/分钟，超限返回 -8
    └──────┬───────┘
           │
           ▼ 通过
    ┌──────────────┐
    │ 检查 manifest│ ◄── 字段是否在 required_fields/optional_fields 中？
    │ 权限范围      │      （复用 manifest.rs 的 field_matches）
    └──────┬───────┘
           │
      否 ◄─┘ 是
      │
      ▼
┌──────────────┐
│ 返回 -1      │
│ 权限不足      │
└──────────────┘

是 ──▶ 解析字段敏感度
           │
    ┌─────┴─────┐
    ▼           ▼
 public/    sensitive/
 internal   critical
    │           │
    ▼           ▼
┌────────┐  ┌──────────────┐
│ 自动从  │  │ 弹出         │
│ Vault  │  │ ConsentDialog │
│ 解密    │  │ (带敏感度色标)│
│ 并返回  │  └──────┬───────┘
└────────┘         │
              ┌────┴────┐
              ▼         ▼
           授权       拒绝
              │         │
              ▼         ▼
         ┌────────┐  ┌────────┐
         │Vault   │  │返回 -2  │
         │解密    │  │UserDenied│
         └────┬───┘  └────────┘
              │
              ▼
         ┌────────┐
         │返回字段值│
         │记录审计  │
         └────────┘
```
