# SoloSoul 全仓瘦身执行报告

> 生成依据：`ponytail-audit` 全仓扫描结果  
> 范围：`tauri/` 前端与 Rust 后端、`solosoul_cli/`、`SoloSoul_plugin_market/`  
> 原则：**只删除死代码 / 合并重复实现 / 用平台能力替换手搓代码**，不引入新功能、不修改业务逻辑。

---

## 一、预期收益

| 项目 | 估算 |
|------|------|
| 可删除 / 合并代码行数 | 约 5,100 行 |
| 可移除直接依赖 | 约 7 个 |
| 主要风险区域 | CLI export/import、窗口状态替换 |

---

## 二、执行阶段

### Phase 0 — 前置检查（必须先做）

```bash
cd /Users/zzc/PycharmProjects/SoloSoul_code

# 1. 确认当前无未提交改动
git status

# 2. 创建独立分支
git checkout -b refactor/ponytail-audit-cleanup

# 3. 跑一遍基线检查
cd tauri && npm run check-all
cd ../solosoul_cli && cargo test && cargo clippy -- -D warnings
```

---

### Phase 1 — 零风险删除（优先执行）

> 目标：先去掉确定无调用方的死代码和未使用依赖，快速减少维护面。

#### 1.1 删除未注册的 Tauri 命令

- **文件**：`tauri/src-tauri/src/commands/fs.rs`
- **删除**：
  - `pub async fn encrypt_file(...)`（约第 91 行起）
  - `pub async fn decrypt_file(...)`（约第 122 行起）
- **验证**：确认 `tauri/src-tauri/src/lib.rs` 的 `generate_handler!` 中没有 `encrypt_file` / `decrypt_file`。
- **替代**：调用方应使用已注册的 `crypto::encrypt_bytes` / `decrypt_bytes`。

#### 1.2 删除死组件与死模块

| 删除目标 | 路径 | 备注 |
|---------|------|------|
| `AttachmentPreview` 组件 | `tauri/src/pages/editor/AttachmentPreview.tsx` | 已被 `AttachmentPreviewOverlay` 替代 |
| CRDT 模块 | `tauri/crates/solosoul-sync/src/crdt.rs` | 仅含单元测试，无生产调用；同时删除 `lib.rs` 中的 `pub mod crdt` |
| MRZ 模板模块 | `tauri/crates/solosoul-core/src/ocr/mrz_templates.rs` | 导出但 `scan_mrz` 与任何命令均未引用；同时删除 `ocr/mod.rs` 中的 `pub mod mrz_templates` |
| `patch_tiers.py` 迁移脚本 | `SoloSoul_plugin_market/scripts/patch_tiers.py` | 一次性脚本，`tier`/`category` 已写入 manifest |

#### 1.3 删除未使用依赖

- **`tauri/package.json`**：删除 `i18next-browser-languagedetector`
- **`solosoul_cli/Cargo.toml`**：
  - 删除 `fs2`（已由 `solosoul-core` 的 `process_lock` 传递提供）
  - 删除 `anyhow`（CLI 源码未实际使用，已用 `color-eyre` + `thiserror`）
- **操作**：删除后执行 `npm install` / `cargo check`。

#### 1.4 删除 `lib.rs` 中的平台探测与弹窗兜底

- **文件**：`tauri/src-tauri/src/lib.rs`
- **删除**：
  - `fn check_webview2_installed()`（约第 102 行起）
  - `unsafe fn show_message_box()`（约第 86 行起）
  - 调用 `show_message_box` 的 panic hook分支（约第 71-75 行）
- **理由**：Tauri 自身会在 WebView2 缺失时失败；panic 信息通过 tracing 与默认输出已足够。

#### 1.5 删除 CLI 图标子系统

- **文件**：`solosoul_cli/src/theme.rs`
- **删除**：
  - `Theme::use_icons` 字段
  - `Theme::icon_or_text()` 方法
  - `should_use_icons()` 函数
  - `SOLOSOUL_CLI_ICONS` 环境变量读取逻辑
- **理由**：TUI 没有任何地方真正渲染图标。

#### 1.6 删除 CLI `upgrade` 占位命令

- **文件**：`solosoul_cli/src/cli.rs`、`solosoul_cli/src/main.rs`
- **删除**：
  - `Commands::Upgrade` 变体及其参数
  - `main.rs` 中针对 `Commands::Upgrade` 的特殊分支
- **理由**：CLI 当前没有升级机制，仅有一个空占位。

---

### Phase 2 — 合并重复实现（中等风险）

> 目标：把两套做同一件事的代码合成一套，降低同步维护成本。

#### 2.1 合并两套 AES-256-GCM 实现 —— ❌ `wontfix`

经过详细评估，`cipher.rs` 与 `aes.rs` **不是重复实现**，而是服务于两个不同协议层的封装，合并收益远低于风险，决定跳过。

**功能对比**

| 维度 | `cipher.rs`（389 行） | `aes.rs`（352 行） |
|------|----------------------|---------------------|
| 数据格式 | 原始 `nonce(12) \|\| ciphertext+tag` | SOLO 格式 `Magic(4) \|\| Ver(1) \|\| nonce(12) \|\| ct+tag` |
| AAD 支持 | ✅ 全部函数支持 | ❌ 不支持 |
| 错误类型 | `CipherError` enum | `String` |
| 单块加密 | `encrypt` / `decrypt` | `encrypt_blob` / `decrypt_blob` |
| 分块加密 nonce | 1 个 base nonce + XOR 派生 per-chunk | 每个 chunk 独立随机 nonce |
| 流式加密签名 | `encrypt_chunked_stream(key, total_size, reader, writer)` | `encrypt_chunked_stream(key, reader, writer, chunk_size)`（需 `Read + Seek`） |
| 返回值 | `EncryptedData` 或 `Vec<u8>` | 始终 `Vec<u8>`（含 SOLO 头部） |

**核心结论**

- `cipher.rs`：通用 AES-256-GCM with AAD，用于 `.solosoul` 导出/导入包。
- `aes.rs`：SOLO 专用 blob 格式，用于 Vault 字段、生物识别文件、前端加密命令等。
- 两种 chunked 格式完全不兼容：
  - `cipher.rs`：`nonce(12) \|\| chunk_count(8) \|\| ct1+tag \|\| ct2+tag \|\| ...`
  - `aes.rs` v3：`SOLO(4) \|\| 0x03(1) \|\| orig_size(8) \|\| chunk_size(4) \|\| chunk_count(4) \|\| nonce1(12) \|\| ct1+tag \|\| ...`

**调用者分析**

- `cipher.rs` 调用者（5+ 文件，均与 export/import 相关）：
  - `solosoul_cli/src/commands/export_import.rs`
  - `tauri/src-tauri/src/commands/export_import/export.rs`
  - `tauri/src-tauri/src/commands/export_import/import.rs`
  - `tauri/crates/solosoul-core/src/pin.rs`
  - export/import 测试文件
- `aes.rs` 调用者（Vault/文件存储）：
  - `tauri/crates/solosoul-vault/src/encryption.rs`
  - `tauri/crates/solosoul-core/src/biometric/legacy.rs`
  - `tauri/src-tauri/src/commands/crypto.rs`
  - `tauri/src-tauri/src/commands/fs.rs`

**评估结论**

| 项 | 评估 |
|----|------|
| 风险 | 🔴 高 — 两种分块格式完全不同，`.solosoul` 文件必须保持向后兼容 |
| 影响范围 | ~15 个文件，±300 行变更 |
| 预期收益 | 仅约 -37 行净减少（核心加解密逻辑都是必要的，重叠很少） |
| 合并难度 | 需要统一格式（破坏兼容性）或保留两种格式（收益几乎为零） |
| 建议 | ❌ 不建议合并 |

**替代方案（如仍想优化）**

1. 轻量整理（低风险）：统一 `cipher.rs` 的 `CipherError` 和 `aes.rs` 的 `String` 错误风格，更新 re-export。变更约 50 行，收益有限。
2. 按兵不动（推荐）：保留两个模块，各自服务不同协议层。

#### 2.2 合并 CLI 重复 helper ✅ 已完成

- **状态**：已于 Phase 3/4 期间提前完成，无需额外变更。
- **现状**：`commands/mod.rs` 已提供 `require_unlocked`、`require_unlocked_with_vault`、`map_err`、`vault` 四个共享函数，所有 22 个命令文件均正确引用。
- **验证**：`cargo test` 通过 ✅，`cargo clippy` 零警告 ✅。

#### 2.3 合并 CLI 列表导航 key handler ✅ 已完成

- **状态**：5 个手动处理 Up/Down/Esc 导航的 handler 迁移为使用 `handle_list_nav`。
- **涉及 handler**：`handle_unlock_key`（SelectAccount）、`handle_new_object_key`（SelectPage + SelectTemplate）、`handle_template_list_key`、`handle_settings_menu_key`
- **收益**：−25 行重复 match 分支。
- **验证**：`cargo test` 通过 ✅，`cargo clippy` 零警告 ✅。
- **剩余**：`handle_trash_list_key`、`handle_plugin_list_key` 等含多键交互，不便于统一。

#### 2.4 合并前端重复工具 ✅ 已完成

| 重复项 | 状态 | 说明 |
|--------|------|------|
| 字节格式化 `formatBytes` | ✅ 已统一 | `attachmentUtils.ts` 从未复制；全部 32 处引用已从 `utils.ts` 导入 |
| i18n prefix 错误解析 | ✅ 已统一 | `utils.ts` 新增 `resolveI18nPrefix`，`backendError.ts`（−15 行）和 `biometricError.ts` 均委托调用 |
| 时间格式化 `formatTimestamp` / `formatRelative` | ✅ 已现代化 | `time.ts` 已使用 `Intl.DateTimeFormat` + `Intl.RelativeTimeFormat`，无手搓实现 |

**验证**：`npx tsc --noEmit` 零错误 ✅，379/379 测试通过 ✅

#### 2.5 合并 LLM 请求构造 ✅ 已完成

- **状态**：已于提交 `5f5a9225` 完成（`refactor(llm): extract shared request construction to request.rs`）。
- **现状**：
  - `request.rs`（143 行）：`build_api_url`、`add_auth_headers`、`build_request_body`、`extract_response_text`、`check_response`、`send_json_request`、`extract_openai_usage`
  - `chat_http.rs`（78 行）：3 个命令全部委托 `request.rs`，仅保留传输层调用
  - `stream.rs`（465 行）：部分使用 `request.rs`（URL 构造、请求体构建、响应提取），SSE 解析保留在 stream.rs

---

### Phase 3 — 架构层瘦身（影响较大，需单独评审）

> 目标：把本应由核心层承担的职责从 CLI 和前端抽出去。

#### 3.1 CLI export/import 下沉到核心 ✅ 已完成

- **CLI 侧**（✅ 已完成）：`solosoul_cli/src/commands/export_import.rs` 从 ~1,772 行减至 ~300 行，完全委托给 `solosoul_core::export_import::{export_vault, import_vault, import_preview}`。CLI 仅保留参数解析、密码提示、路径解析、终端输出。
- **Core 侧**（✅ 已完成）：`tauri/crates/solosoul-core/src/export_import.rs` ~850 行，包含完整的导出/导入业务逻辑 + 丰富测试（8 个测试：完整导出、正确/错误密码、模板快照、去重导入、自定义页面等）。
- **GUI 侧**（⏭️ 可单独优化）：`tauri/src-tauri/src/commands/export_import/` 仍有独立实现，包含 GUI 专属功能（模板字段注入、偏好/行为数据导出、附件细粒度选择），不建议一次性强制统一。

#### 3.2 CLI vault_write / attachment 下沉 ✅ 已完成

- **状态**：已于提交 `0294fe6f` 完成（`refactor(core): Phase 3 complete - CLI export/import and vault_write/attachment moved to solosoul-core`）。
- **Core 侧**（✅ 已完成）：`tauri/crates/solosoul-core/src/objects.rs` ~550 行，包含完整的对象/附件/回收站业务逻辑 + 8 个单元测试。
  - `create_page()`、`create_object()`、`update_object()`
  - `move_to_trash()`、`restore_from_trash()`、`purge_trash()`
  - `add_attachments()`、`rename_attachment()`、`soft_delete_attachment()`、`restore_attachment()`、`purge_attachment()`、`cleanup_orphan_attachments()`
  - `load_trash_retention()`、`object_record_from_trash()`
- **CLI 侧**（✅ 已完成）：
  - `vault_write.rs`（从 ~1,202 行减至 ~220 行）：仅保留向导状态管理、参数解析、确认弹窗、结果展示
  - `attachment.rs`（从 ~889 行减至 ~160 行）：仅保留命令分发、对象 ID 推断、确认弹窗、结果展示
- **验证**：`cargo test`（CLI 2 ✅ / Core 119 ✅）、`cargo clippy` 零警告 ✅

#### 3.3 替换窗口状态管理 ✅ 已完成

- **状态**：已于提交 `16503158` 完成（`feat(tauri): replace custom window-size hook with plugin-window-state (Phase 3.3)`）。
- **操作**：
  1. ✅ `@tauri-apps/plugin-window-state` 已安装（`^2.4.1`）
  2. ✅ `useWindowSize.ts` 已删除
  3. ✅ 插件已注册：`tauri_plugin_window_state::Builder::new().build()`
  4. ✅ `ST_WINDOW_SIZE` 相关 storage key 和 UI/account preference 已清理（全仓 0 匹配）
- **Cargo 依赖**：✅ `tauri-plugin-window-state = "2"` 已添加

#### 3.4 插件 SDK helper 统一 ✅ 已完成

- **状态**：3 项子任务均已提前完成。
  - `escape_json` / `truncate`：已在 SDK lib.rs 中存在（`result_text`、`result_key_value`、`result_table` 等使用 `sdk::escape_json`）。
  - 插件本地副本：全仓搜索 0 匹配，插件从未各自实现这些函数（使用 SDK 的 `format!` 语法构建 JSON）。
  - 异步 HTTP 表面：`HttpRequest`、`HttpPollState`、`post_json_async` 全仓 0 匹配，已删除。

---

### Phase 4 — 小修小补清单 ✅ 已完成

> 全部 9 项可执行任务已完成并推送（commit `5cfcd6aa`）。

| # | 任务 | 状态 | 收益 | 说明 |
|---|------|------|------|------|
| 1 | `ipc.ts` commands 对象 | ✅ 完成 | −170 行 | **用户选择删除**（非标注保留）。38 个方法全部迁移为 `invoke()`，14 个文件更新 |
| 2 | `useWorkspacePasswordGuard` hook 内联 | ✅ 完成 | −30 行 | 内联到 `ObjectWorkspacePage.tsx`，源文件已删除 |
| 3 | 微工具文件合并（7→2） | ✅ 完成 | −120 行 | `utils.ts` + `constants.ts`，format/env/ocr/pageLabels/iconSizes/storageKeys 6 个文件已删除 |
| 4 | CLI settings 选择屏幕合并 | ✅ 完成 | −200 行 | 泛型 `settings_select.rs`，两个特定屏委托调用 |
| 5 | `command_usage` 从 `HELP_GROUPS` 派生 | ✅ 完成 | −40 行 | `LazyLock<HashMap>` 替代 40+ 行 match，保留完整用法文本 |
| 6 | Rust SDK 字符串读取统一 | ✅ 完成 | −70 行 | 9 个函数统一走 `read_string_from_host`；`show_dialog` 因不同错误映射保留原样 |
| 7 | `PluginSession` + `PluginSessionInfo` 合并 | ✅ 完成 | −30 行 | `PluginSessionInfo` 删除，serde rename 保证 JSON 兼容 |
| 8 | SyncManager sessions map 删除 | ✅ 完成 | −50 行 | `PeerSession` struct + `sessions` 字段 + 所有写入点已删除 |
| 9 | `pdfium.rs` OnceLock 化 | ⏭️ 跳过 | — | 代码库中不存在 pdfium 相关代码 |
| 10 | `update-registry.yml` 冲突 | ⏭️ 已解决 | — | 该 workflow 已不存在；CI 使用验证模式（diff --quiet），与 AGENTS.md 一致 |

**提交**：`5cfcd6aa` — `refactor: Phase 4 — codebase cleanup (9/9 items complete)`
**验证**：`npx tsc --noEmit` 零错误 ✅，379/379 测试通过 ✅，SDK `cargo build` + `cargo test` 通过 ✅

---

## 三、验证 checklist

每完成一个 Phase 都要执行：

```bash
# Tauri 前端
cd tauri
npm run check-all

# Tauri Rust
cargo fmt --check
cargo clippy -- -D warnings
cargo test --verbose

# CLI
cd ../solosoul_cli
cargo fmt --check
cargo clippy -- -D warnings
cargo test

# 插件市场
cd ../SoloSoul_plugin_market
python3 scripts/generate_registry.py
git diff --exit-code registry.json
```

---

## 四、建议的提交粒度

| 提交 | 内容 |
|------|------|
| `chore: remove dead commands and modules` | Phase 1 所有删除 |
| `refactor: deduplicate CLI helpers and key handlers` | Phase 2.2 / 2.3 |
| `refactor: unify frontend formatters and error helpers` | Phase 2.4 |
| `refactor: extract shared LLM request builder` | Phase 2.5 |
| `refactor(cli): move export/import orchestration to core` | Phase 3.1（单独 PR） |
| `refactor(cli): move object/attachment write logic to core` | Phase 3.2（单独 PR） |
| `feat(tauri): replace custom window-size hook with plugin-window-state` | Phase 3.3（单独 PR） |
| `refactor(plugins): share sdk helpers and remove async http surface` | Phase 3.4（单独 PR） |
| `chore: minor cleanups from ponytail-audit` | Phase 4 |

---

## 五、注意事项

1. **不要一次性做完所有改动**。Phase 3 每一项都建议单独 PR，避免 review 爆炸。
2. **Phase 2.1 已评估为 `wontfix`**。`cipher.rs` 与 `aes.rs` 分别服务不同协议层，合并会破坏 `.solosoul` 导出/导入格式或收益几乎为零，故保留现状。
3. **窗口状态替换要人工验证**：首次启动、多账户切换、退出重开三种场景。
4. **插件市场修改后必须本地重新生成 `registry.json`**，否则 `validate-registry.yml` 会失败。
5. **所有删除操作前先 `git grep` 确认没有隐藏调用方**，尤其是动态字符串拼接的命令名。
